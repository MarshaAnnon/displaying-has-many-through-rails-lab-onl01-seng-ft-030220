<h1>Appointments for <%= @patient.name %></h1>
<div>
    <% @patient.appointments.each do | app | %></h4>
        <h4>Date/Time:<%= app.appointment_datetime.strftime('%B %d, %Y at %H:%M') %></h4> 
        <h4>Doctor:<%= link_to app.doctor.name, doctor_path(app.doctor) %>
    <% end %>
</div>